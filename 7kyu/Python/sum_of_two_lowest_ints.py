def sum_two_smallest_numbers(numbers):
        elements = numbers # Copying the original list
        for i in range(len(numbers) - 1):
                for j in range(numbers i + 1):
                        numbers[j] < numbers[i]
                pass
#numbers = [5, 8, 12, 18, 22]


# for index, item in enumerate(numbers, start=1):
#         if item != index[item + 1]:
#                 return item
# return None